# ============================================================================
# Serventis Framework - Global Configuration Defaults
# ============================================================================
#
# This file contains framework-level defaults for all circuits.
# Override in circuit-specific files (config_{circuit}.properties)
# or container-specific files (config_{circuit}-{container}.properties)
#
# Version: 1.0.0-SNAPSHOT
# Created: 2025-10-26
#
# ============================================================================

# -----------------------------------------------------------------------------
# Health Threshold Configuration (Global Defaults)
# -----------------------------------------------------------------------------
# These values match hardcoded constants
health.thresholds.heap.stable=0.75
health.thresholds.heap.degraded=0.90
health.thresholds.cpu.stable=0.70
health.thresholds.cpu.degraded=0.85

# -----------------------------------------------------------------------------
# Signal Processing Configuration
# -----------------------------------------------------------------------------
signals.batch-size=100
signals.flush-interval-ms=1000

# -----------------------------------------------------------------------------
# VectorClock Configuration
# -----------------------------------------------------------------------------
vector-clock.enabled=true
causality.tracking-enabled=true
