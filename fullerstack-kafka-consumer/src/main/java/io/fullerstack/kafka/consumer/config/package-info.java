/**
 * Consumer-specific configuration models.
 * <p>
 * This package may contain consumer-specific configuration in the future.
 * Currently, configuration is handled via {@link io.fullerstack.kafka.core.config.ConsumerSensorConfig}.
 */
package io.fullerstack.kafka.consumer.config;
