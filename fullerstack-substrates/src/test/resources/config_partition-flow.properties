# Partition Flow Circuit Configuration
valve.queue-size=500
signals.batch-size=50
health.thresholds.heap.stable=0.60

# Circuit structure definition
circuit.containers=partitions
circuit.conduits=metadata-changes
